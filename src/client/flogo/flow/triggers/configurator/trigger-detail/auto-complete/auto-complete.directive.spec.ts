import { AutoCompleteDirective } from './auto-complete.directive';

xdescribe('AutoCompleteDirective', () => {
  it('should create an instance', () => {
    // const directive = new AutoCompleteDirective();
    // expect(directive).toBeTruthy();
  });
});
