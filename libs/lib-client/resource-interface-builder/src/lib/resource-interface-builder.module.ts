import { NgModule } from '@angular/core';

import { ResourceInterfaceBuilderService } from './resource-interface-builder.service';

@NgModule({
  providers: [ResourceInterfaceBuilderService],
})
export class ResourceInterfaceBuilderModule {}
