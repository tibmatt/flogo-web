import gulp from 'gulp';
/**
 * Watch server and client sources and execute the dev tasks when they change
 */
gulp.task('dev.watch', 'Watch server and client sources and execute the dev tasks when they change', ['dev.server.watch']);
